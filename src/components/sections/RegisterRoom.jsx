import styles from './Placeholder.module.css';

export default function RegisterRoom() {
  return (
    <section id="register-room" className={styles.section}>
      <h2 className={styles.heading}>Register Room</h2>
      {/* TODO: refer to canva page, doc specs */}
      <p className={styles.placeholder}>[ placeholder — Person B ]</p>
    </section>
  );
}