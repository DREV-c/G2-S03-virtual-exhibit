import styles from './Placeholder.module.css';

export default function DualSRIFailure() {
  return (
    <section id="dual-sri-failure" className={styles.section}>
      <h2 className={styles.heading}>Dual SRI Failure</h2>
      {/* TODO: refer to canva page, doc specs */}
      <p className={styles.placeholder}>[ placeholder — Person B ]</p>
    </section>
  );
}