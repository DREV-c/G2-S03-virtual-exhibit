import styles from './Placeholder.module.css';

export default function AboutBinary() {
  return (
    <section id="about-binary" className={styles.section}>
      <h2 className={styles.heading}>About Binary</h2>
      {/* TODO: binary strip visual, info about binary */}
      <p className={styles.placeholder}>[ placeholder — Person B ]</p>
    </section>
  );
}